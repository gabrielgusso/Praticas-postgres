SELECT u.id, u.name, r.name AS role, c.name AS company, e."startDate" from experiences e JOIN users u ON u.id = e."userId" JOIN roles r ON r.id = e."roleId" JOIN companies c ON c.id = e."companyId" WHERE u.id = 50 AND e."endDate" IS NULL;